<%= form_with(model: technician, local: true) do |form| %>
  <% if technician.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(technician.errors.count, "error") %> prohibited this technician from being saved:</h2>

      <ul>
      <% technician.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<form>
  <div class="form-group">
    <%= form.label :tech_name %><br>
    <%= form.text_field :tech_name, id: :technician_tech_name %>
  </div>

  <div class="form-group">
    <%= form.label :technician_status_id %>
    <%= form.collection_select :technician_status_id, TechnicianStatus.all, :id, :tech_status %>
  </div>

  <div class="form-group">
       <%= form.text_area :tech_expertise_notes, placeholder: 'tech expertise notes' %>
  </div>
</form>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
